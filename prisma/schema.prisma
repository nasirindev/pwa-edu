generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Student {
  id         Int      @id @default(autoincrement())
  fullName   String
  schoolName String
  grade      String
  createdAt  DateTime @default(now())
}
